<sub>ssheleg skills — task-pipeline · evidence-docs · quest-store</sub>

# Release candidate continuation — 25 September 2026

The operator asked to finish release preparation under the new GitHub Actions limits.
This resumes the [original brief](2026-09-24-brief.md) and [release report](../release-review-2026-09-24.md).
The executable APK remains the signed 0.1.0-rc.1 candidate; no runtime code changed today.

## CI policy applied

The local update command was run in Dataroom, the client and workspace; these checkouts
were not enrolled and returned no notice. The enrolled Observatory checkout printed
`ci-batch-2026-09-25-v1`. Its exact linked policy was read, then acknowledged for the
current stable Codex session ID. This says nothing about other sessions' acknowledgement.
No unrelated dirty files in Observatory were changed.

The operator instruction applies here even though VR had no local notice configuration.
At client main `2fc5691ab741cbf89616ac6b8aafed82a24668df`, full builds still ran on push/PR.
The isolated [CI policy PR #1](https://github.com/nicegram/Nicegram-VR/pull/1) is merged:
`eb6bee6da9b0631b8f7e78f61ffa28221faa2792`. The candidate branch merged that revision,
without resetting, rebasing or losing its runtime fixes. See [the policy](../CI-BATCHING.md).

- Full build: 23:00 Europe/Warsaw, default-branch snapshot; manual dispatch retained.
- Existing build/test steps, native cache, 120-minute timeout and per-ref cancellation retained.
- New workflow artifacts retained seven days. Draft release assets remain available.
- No full hosted suite dispatched in this continuation. The first scheduled run is unverified.
- Unmerged runtime fixes are outside the nightly snapshot. The policy PR does not integrate them.

## Checks actually run

- actionlint 1.7.12 on both workflows: exit 0; syntax/expression validation, shellcheck disabled.
- Parsed YAML before/after comparison: original job, steps and concurrency unchanged after
  removing the added retention field; exact schedule/manual trigger set verified. Release
  workflow unchanged by the policy PR. Comparison base: `2fc5691a`.
- `python3 -m unittest discover -s Tools/tests -v`: eight passed, zero failed.
- `python3 Tools/check_docs.py`: exit 0. This checks references/counts, not every semantic claim.
- `git diff 14935de9 -- TMessagesProj TMessagesProj_AppQuest gradle.properties build.gradle settings.gradle`:
  empty. The APK build inputs were not modified; no new APK build or JVM-suite rerun was needed.
  The 97 passing JVM tests belong to the September 24 receipt, not a new hosted run.
- `shasum -a 256` on the retained candidate matches the [APK receipt](2026-09-24-apk.json):
  `11ef5e258595fa2711c8771b5779919b5348e95d8e0e0c0d38d91680b4d2a70a`.
- `gh release view v0.1.0-rc.1`: still draft, three uploaded assets. The server APK digest
  matches the local receipt. No public release tag pushed.
- `adb devices -l`: empty. Physical Quest acceptance remains **NOT-RUN**.
- `metavr auth status --validate`: authenticated and valid. This does not prove organization
  membership, app permissions or a Store-ready application; token scope remains unknown.
- Browser tooling had no available browser; native Chrome access timed out. Dashboard state
  could not be inspected. No authentication values or personal account details are stored here.

## Remaining work and exact next task

The missing prerequisites were requested from the operator: a connected Quest 3/3S and the
Meta organization/app ID or Dashboard URL. While they are unavailable, there is no evidence
for cold launch, speech/calls, sleep, multi-account behavior, upgrade preservation or channel
installation. A GitHub budget change cannot stand in for those checks.

Next: connect the Quest, verify the candidate hash, install with `adb install -r` only when
signatures are compatible, and complete [device-session](../device-session.md). Never erase
an existing session to bypass a signing conflict. Then inspect the identified Meta app and
continue steps 3–9 of the release checklist, including policy questions and real screenshots.
No Meta upload, reviewer submission, support message or public publication was performed.

The runtime candidate stays separate from the merged CI policy. Keep local caches, signing
material, logs and machine notice receipts out of Git. Dataroom owns the cross-repository
handoff; the private product workspace still owns canonical scenarios.

---

**Made with [ssheleg skills](https://github.com/ssheleg/sshlg-skills)**

- [`task-pipeline`](https://github.com/ssheleg/task-pipeline) — CI policy adoption and integration
- [`evidence-docs`](https://github.com/ssheleg/task-pipeline) — release evidence and handoff
- [`quest-store`](https://github.com/ssheleg/xr-dev) — remaining publication prerequisites
